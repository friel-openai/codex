test: compile app-server fixtures after upstream 0.154 rebase

Retain one initializer per field and expect the independently recorded goal turn. Production files and selected regression assertions are unchanged.
